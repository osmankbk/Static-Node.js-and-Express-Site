extends layout.pug

block content
  article.grid-container.portfolio-intro
    div

      //- My Title
      h1 The Curious Arsenal

      //- A brief description of my portfolio, what this project is, it's purpose, what it's supposed to accomplish.
      p.lead.text-light
        | This Portfolio is meant to give a glimpse of who I am, why I'm pursuing a career in programming and to demonstrate  my skills and experiences I've acquired on this awesome journey as a developer, so far.

  article.grid-container.portfolio-index

    .grid-x.grid-margin-x.small-up-2.medium-up-2.large-up-3

      //- I iterate over each project to create a project img, title and link for each of the projects in my showcase.

      //- And I use a combination of interpolation and template literals to dynamically generate the routes in the href, as well as the img's src and the projects title.
      each project in projects
          .cell
            a(href=`project/${project.id}`)
              img.thumbnail(src=`${project.image_urls[0]}`)
              h5= project.project_name
